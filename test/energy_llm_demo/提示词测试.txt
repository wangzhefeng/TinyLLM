请生成一段基于scipy.optimize.minimize求解的优化代码，并把所有建模求解的过程包含在一个叫opt_run的函数中，这个函数有三个返回，分别是优化求解是否成功，优化求解的函数值和优化求解的变量值。其中这个优化求解问题的目标是最小化一个叫hvac_load的值，优化的变量为ch_t，hvac_load分别与ch_t和一个定量weather有线性关系，weather的值为22，ch_t和weather的权重为-4.22和11.58，偏置为-19.75。优化变量ch_t的上下限为7到12，并有一个限制条件优化目标hvac_load的值必须大于6。